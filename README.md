# delta-capita-assessment

Steps to Run:
- Install latest version of Node.js
- Clone the repository in local system
- Install VS code in your system
- To Install dependencies run command
  npm install
- Run all tests
  npx playwright test
